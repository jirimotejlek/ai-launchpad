# ChromaDB Configuration (Optional)
# ChromaDB works without additional configuration by default
# Copy this file only if you need custom settings:
# cp env_templates/chroma.env .env.chroma

# ChromaDB Connection
CHROMA_HOST=chroma
CHROMA_PORT=8000

# Note: ChromaDB doesn't require credentials for basic usage
# All configuration is handled via docker-compose.chroma.yml

